# TeleBackuper 🗄️📦

A modular CLI backup manager for Linux servers with Telegram notifications.

## Features
- Multiple independent backups
- Per-backup configuration
- Cron-based scheduling
- Telegram notifications
- Enable / Disable backups
- Tar.gz compression

## Installation
```bash
git clone https://github.com/SamanGhn/TeleBackuper.git
cd TeleBackuper
sudo bash install/prerequisites.sh
